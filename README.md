DEEPAMM ENGINEERING ENTERPRISES — WEBSITE SOURCE
===================================================

Source version inspected:
AppDeploy app: deepam-engineering-enterprises-a03ah9
Version: 1790255220172 (latest valid/applied source snapshot)

What was checked before packaging:
- AppDeploy deployment status: READY
- Frontend errors: none reported
- Backend errors: none reported
- AppDeploy QA snapshot: no frontend/network errors reported
- Mobile navigation test definition includes all 8 pages
- Mobile home/background and 3D title behavior are included
- Product 3D showcase uses component1.png with 20-second Y-axis rotation
- Secondary Operations uses secondary-operations-drilling.png
- Email address is dee@deepameng.com
- Contact addresses and Aironotical entry are present
- Install prompt suppression is present in src/main.tsx

IMPORTANT:
This ZIP is the website source plus the locally available image assets used by the current site.
The Industries Served customer-logo strip still references several external logo URLs, so those logos are not local files in this package.

To run:
  npm install
  npm run dev

To build:
  npm run build

The source is a Vite + React + TypeScript website.
